// Первая часть
let num = +prompt('Введите число');
let a = +prompt('Введите степень этого числа');
let b = num**a  
let res = alert('Результат: ' + b);
// Первая часть окончена

// Вторая часть

let x = +prompt('Введите число');
let y = prompt('Введите символ');
let z = prompt('Введите конечный символ');
let ref = ''
for (let i = ''; i <x; i++) {
if (i == 0) {
  ref == z
}else{
  ref +=y 
}
  console.log(ref + z);
}

  // Вторая часть окончена